# Usage
```
Options:
  --internal-database=true|false  If use this option, should provide database file path
  --database-file-path=<text>     The path of internal database file
  --database-uri=<text>           The uri of database
  --export-path=<text>            The path of export database file
  -h, --help                      Show this message and exit
```